{
  "skill_name": "dokpilot",
  "evals": [
    {
      "id": 1,
      "prompt": "/dokpilot deploy github.com/your-name/my-nextjs-app --domain app.example.com",
      "expected_output": "The skill should: 1) Read deploy-guide.md (not search web), 2) Clone repo and detect Next.js stack, 3) Create project with environmentId, 4) Set up DNS with --no-proxy, 5) Deploy via API, 6) After deploy, mention that auto-deploy is active via GitHub App (NOT suggest webhook setup), 7) NOT search the web for Dokploy docs",
      "files": [],
      "assertions": [
        {
          "name": "no_web_search_for_docs",
          "type": "negative",
          "description": "Must NOT use WebSearch or WebFetch to look up Dokploy documentation"
        },
        {
          "name": "reads_reference_files",
          "type": "positive",
          "description": "Must read deploy-guide.md and/or stack-detection.md from references/"
        },
        {
          "name": "no_webhook_suggestion",
          "type": "negative",
          "description": "Must NOT suggest setting up GitHub webhooks, refresh tokens, or GitHub Actions for auto-deploy"
        },
        {
          "name": "mentions_github_app_autodeploy",
          "type": "positive",
          "description": "Must mention that auto-deploy works via GitHub App and push to branch triggers deployment"
        },
        {
          "name": "uses_environment_id",
          "type": "positive",
          "description": "Must use environmentId when creating application (v0.27+ requirement)"
        },
        {
          "name": "dns_no_proxy",
          "type": "positive",
          "description": "Must create DNS record with --no-proxy flag for Let's Encrypt HTTP challenge"
        }
      ]
    },
    {
      "id": 2,
      "prompt": "My app deployed earlier stopped updating when I push to main. How do I fix auto-deploy? Maybe I need to set up a webhook?",
      "expected_output": "The skill should: 1) Read github-app-autodeploy.md reference, 2) Explain that webhooks are NOT needed with GitHub App, 3) Walk through troubleshooting: check GitHub App installation, check autoDeploy flag, check branch config, 4) NOT suggest creating webhook URLs or adding them to GitHub",
      "files": [],
      "assertions": [
        {
          "name": "no_webhook_suggestion",
          "type": "negative",
          "description": "Must NOT suggest adding a webhook to GitHub repository"
        },
        {
          "name": "explains_github_app",
          "type": "positive",
          "description": "Must explain that GitHub App handles auto-deploy, webhooks not needed"
        },
        {
          "name": "troubleshooting_steps",
          "type": "positive",
          "description": "Must suggest checking: GitHub App installation, autoDeploy flag, branch config"
        },
        {
          "name": "no_web_search",
          "type": "negative",
          "description": "Must NOT search the web for how auto-deploy works in Dokploy"
        }
      ]
    },
    {
      "id": 3,
      "prompt": "/dokpilot setup 203.0.113.11 MyR00tPass456",
      "expected_output": "The skill should: 1) Read setup-guide.md (not search web), 2) SSH to server, check resources, 3) Install Dokploy, 4) Wait for Dokploy to become available, 5) Ask user to create admin account in UI and provide API key, 6) On macOS, offer Keychain as the default storage for the API key (user can opt for plain file), 7) Save server config to servers.json with {\"_secret\": ...} reference when Keychain is chosen, 8) Report completion with next steps",
      "files": [],
      "assertions": [
        {
          "name": "reads_setup_guide",
          "type": "positive",
          "description": "Must read references/setup-guide.md"
        },
        {
          "name": "no_web_search",
          "type": "negative",
          "description": "Must NOT search the web for how to install Dokploy"
        },
        {
          "name": "checks_ssh_access",
          "type": "positive",
          "description": "Must attempt SSH connection to verify access"
        },
        {
          "name": "asks_for_api_key",
          "type": "positive",
          "description": "Must ask user to create admin account in UI and provide API key (not try to create via API)"
        },
        {
          "name": "offers_keychain_default",
          "type": "positive",
          "description": "On macOS, must offer Keychain as the default secret store for the Dokploy API key and only fall back to plain file when the user explicitly declines"
        },
        {
          "name": "stores_secret_reference",
          "type": "positive",
          "description": "When Keychain is used, servers.json must contain {\"_secret\": \"<account>\"} instead of the raw token"
        },
        {
          "name": "no_token_in_stdout",
          "type": "negative",
          "description": "Must NOT print the API key value back to the user at any point"
        }
      ]
    },
    {
      "id": 4,
      "prompt": "/dokpilot config migrate-to-keychain",
      "expected_output": "The skill should: 1) Read references/secrets-management.md, 2) Detect which secret fields in servers.json are plain strings, 3) Write a backup file config/servers.json.pre-keychain-<date>, 4) For each plain secret, call scripts/secret-store.sh set with the correct account naming convention (<server>:<field> or cloudflare:<field>), 5) Rewrite servers.json so every migrated field becomes {\"_secret\": \"<account>\"}, 6) Leave already-referenced fields untouched, 7) Print a summary report without printing any secret values",
      "files": [],
      "assertions": [
        {
          "name": "reads_secrets_management_guide",
          "type": "positive",
          "description": "Must read references/secrets-management.md or the migration section of SKILL.md"
        },
        {
          "name": "creates_backup_file",
          "type": "positive",
          "description": "Must create config/servers.json.pre-keychain-<timestamp> before rewriting"
        },
        {
          "name": "uses_secret_store_script",
          "type": "positive",
          "description": "Must call scripts/secret-store.sh set for each plain secret (not raw `security` commands)"
        },
        {
          "name": "rewrites_to_reference_form",
          "type": "positive",
          "description": "After migration, secret fields in servers.json must be objects of the form {\"_secret\": \"<account>\"}"
        },
        {
          "name": "no_secret_values_in_output",
          "type": "negative",
          "description": "Must NOT print raw token values in the summary or anywhere else"
        }
      ]
    },
    {
      "id": 5,
      "prompt": "/dokpilot status",
      "expected_output": "On an installation whose servers.json still has plain-string tokens (legacy format), the skill should: 1) Treat the config as valid and proceed, 2) Call dokploy-api.sh — which uses resolve_secret() — and succeed against the string value, 3) NOT print a deprecation warning for the plain-string form, 4) NOT refuse to run or demand migration",
      "files": [
        {
          "path": "config/servers.json",
          "content_hint": "servers.main.dokploy_api_key is a plain string, cloudflare.api_token is a plain string"
        }
      ],
      "assertions": [
        {
          "name": "accepts_legacy_config",
          "type": "positive",
          "description": "Must run /dokpilot status successfully against a config with plain-string tokens"
        },
        {
          "name": "no_deprecation_warning",
          "type": "negative",
          "description": "Must NOT print a warning or error about plain-string storage being deprecated"
        },
        {
          "name": "no_forced_migration",
          "type": "negative",
          "description": "Must NOT block the command or require the user to migrate before status can run"
        }
      ]
    },
    {
      "id": 6,
      "prompt": "/dokpilot status",
      "expected_output": "Against a Dokploy server whose version differs from the skill's documented baseline (v0.29+), the skill should: 1) Call settings.version via scripts/dokploy-api.sh to discover the running Dokploy version, 2) Compare against the documented baseline, 3) If the running version is BELOW baseline, print an actionable warning naming both versions and pointing to references/dokploy-api-reference.md, 4) If the running version is AT OR ABOVE baseline, proceed silently, 5) Never crash on a version mismatch — the warning is informational, status output is still produced",
      "files": [
        {
          "path": "config/servers.json",
          "content_hint": "servers.main.dokploy_url points to a Dokploy instance whose settings.version returns a string like 'v0.26.0' (below baseline) or 'v0.29.5' (at baseline)"
        }
      ],
      "assertions": [
        {
          "name": "calls_settings_version",
          "type": "positive",
          "description": "Must call scripts/dokploy-api.sh <server> GET settings.version (or equivalent) at least once during status execution"
        },
        {
          "name": "reads_documented_baseline",
          "type": "positive",
          "description": "Must determine the documented baseline either from references/dokploy-api-reference.md or from the skill's SKILL.md/README"
        },
        {
          "name": "warns_on_below_baseline",
          "type": "positive",
          "description": "When the running version is older than the baseline, must print a warning that names BOTH versions ('Detected Dokploy v0.26.0. This skill targets v0.29+.') and references the docs"
        },
        {
          "name": "silent_on_at_or_above",
          "type": "negative",
          "description": "When the running version is at or above the baseline, must NOT print a version-mismatch warning"
        },
        {
          "name": "never_crashes_on_mismatch",
          "type": "positive",
          "description": "Must produce a normal status output regardless of version mismatch — the version check is informational, not a hard gate"
        }
      ]
    }
  ]
}
