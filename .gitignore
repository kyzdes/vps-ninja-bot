# Credentials
config/servers.json

# Dependencies
node_modules/
mcp-server/node_modules/

# Build output
landing/.next/

# OS
.DS_Store

# Editor
*.swp
*.swo
*~

# managed by context-map skill — do not edit between markers
# context-map-<slug>/ folders are project memory for AI coding agents.
# They are not source code and may contain private operational notes, decisions,
# and known issues that must not be pushed to public hosting.
context-map-*/
# end context-map skill section

# Local-only working folders (not part of skill release)
landing/
.antigravitycli/
.playwright-mcp/
/.brand-tmp/

# Open Design scratch artifacts (one-shot generations land at repo root)
/.od-skills/
/dokpilot-ui.html
/dokpilot-ui.html.artifact.json
/mpjsy0ar-image.png

# ui-server runtime — log lives in ~/.claude/skills/dokpilot/ but
# during dev the symlink can resolve into the repo; never commit it
skills/dokpilot/ui-server.log
skills/dokpilot/.ui-pid
skills/dokpilot/.ui-port
skills/dokpilot/.ui-url
skills/dokpilot/jobs/
skills/dokpilot/destroy-nonces/
